;need an activator function (toggles?)

;a timer function that is called by activator
; this function should handle sending a trade rep request and then waiting for a window pop up, perhaps by checking pixels
; when the window exists to click the accept button on the menu, then waiting the cool down time and starting again

;a function to send trade rep request

;